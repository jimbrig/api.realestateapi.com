extends:
  - recommended
apis:
  reapi:
    root: openapi/openapi.yml
  reapi-swagger:
    root: swagger.json
openapi:
  generateCodeSamples:
    languages:
      - lang: curl
      - lang: Python
      - lang: R
